all:
	@echo "You are in a matching environment for a specific function. Only use the tools explicitly listed in this directory's CLAUDE.md. If you're ready to build against the main project, you need to jump back two directory levels (cd ../..)"

%: all
	@
